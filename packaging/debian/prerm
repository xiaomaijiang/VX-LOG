#!/bin/bash

set -e

case "$1" in

	remove)
	    if test -f /var/run/nxlog/nxlog.pid; then
		/etc/init.d/nxlog stop
	    fi
	;;

	*)
	;;

esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0

